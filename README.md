# Наряд

Android-приложение для учёта рабочих смен, выполненных операций, нормо-часов и зарплаты. Данные хранятся локально на устройстве; приложение рассчитано на ежедневное ведение наряда и последующий месячный отчёт.

Текущая версия: **3.2-beta**.

## Возможности

### Наряды и операции

- отдельный наряд на каждый день;
- добавление операций партиями, например `+10` или `+17`;
- журнал добавлений с количеством и временем каждой записи;
- комментарии к рабочему дню;
- изменение длительности смены и ручное назначение типа дня;
- каталог операций с нормой времени, разрядом от 3 до 6 и настраиваемым порядком;
- редактирование названия, нормы времени и разряда без удаления истории выполненных операций.

### Календарь и графики работы

- график 5/2;
- график 2/2 со сдвигаемой датой начала цикла;
- автоматическое обозначение выходных;
- ручные режимы «Рабочий» и «Праздник ×2»;
- отображение заполненных смен, часов и работы в выходные.

### Зарплата

- полная зарплата, аванс и вторая часть;
- отдельная договорная ставка для аванса;
- учёт месячной нормы часов;
- двойная оплата выходных и праздничных смен;
- разделение задержек на обычные и оплачиваемые в двойном размере;
- прогноз зарплаты на месяц по графику;
- автоматическое уточнение прогноза по мере заполнения переработок и выходов.

### Экспериментальный банк операций

Банк позволяет переносить часть выполненных операций между расчётными месяцами, не изменяя исходные наряды и журнал.

Доступны режимы:

- пропорциональное распределение;
- приоритет по разрядам;
- заданное процентное соотношение разрядов;
- ручное распределение.

Лимит рассчитывается по формуле:

```text
фактические часы присутствия × коэффициент лимита
```

### Отчёты и перенос данных

- месячная сводка по количеству операций и нормо-часам;
- диаграммы операций и процентного соотношения разрядов;
- экспорт отчётов в PDF и CSV;
- экспорт и импорт каталога операций и настроек через файл
  `Документы/WorkOrder/work_order_settings.json`.

## Технологии

- Kotlin;
- Jetpack Compose и Material 3;
- MVVM;
- Room с явными миграциями схемы;
- Coroutines и Flow;
- Navigation Compose;
- Gson, iText 7 и OpenCSV.

## Требования

- Android 8.0 или новее — API 26+;
- Android SDK 34 для сборки;
- JDK 17;
- актуальная версия Android Studio с поддержкой Kotlin и Compose.

## Сборка

Открой проект в Android Studio и дождись синхронизации Gradle либо выполни в PowerShell:

```powershell
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
.\gradlew.bat testDebugUnitTest assembleDebug
```

Готовый debug APK появится здесь:

```text
app/build/outputs/apk/debug/app-debug.apk
```

Установка на подключённое устройство:

```powershell
adb install -r app\build\outputs\apk\debug\app-debug.apk
```

## Тесты

В проекте есть модульные тесты расчёта зарплаты, графика 2/2 и банка операций, а также Android-тесты сохранности записей при редактировании операций.

```powershell
.\gradlew.bat testDebugUnitTest
```

## Хранение данных

- основная база Room хранится во внутреннем хранилище приложения;
- банк операций является отдельным расчётным слоем и не переписывает наряды;
- резервные базы, экспортированные настройки, локальные SDK-пути и собранные APK исключены из Git через `.gitignore`.

Перед экспериментами с банком операций рекомендуется сделать экспорт настроек и резервную копию данных приложения.

## Структура проекта

```text
app/src/main/java/com/workorder/app/
├── data/          # Room-модели, DAO и репозитории
├── domain/        # расчёт зарплаты
├── ui/            # Compose-экраны, компоненты и ViewModel
└── util/          # импорт, экспорт и форматирование
```

История изменений находится в [CHANGELOG.md](CHANGELOG.md).

